---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sandboxclaims.demo.example.com
spec:
  group: demo.example.com
  names:
    kind: SandboxClaim
    listKind: SandboxClaimList
    plural: sandboxclaims
    singular: sandboxclaim
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxClaim is the Schema for the sandboxclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SandboxClaim
            properties:
              templateRef:
                description: TemplateRef references the SandboxTemplate to instantiate.
                properties:
                  name:
                    description: Name is the name of the SandboxTemplate.
                    type: string
                required:
                - name
                type: object
            required:
            - templateRef
            type: object
          status:
            description: status defines the observed state of SandboxClaim
            properties:
              phase:
                description: Phase mirrors the phase of the created Sandbox.
                type: string
              sandboxName:
                description: SandboxName is the name of the Sandbox created for this
                  claim.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sandboxwarmpools.demo.example.com
spec:
  group: demo.example.com
  names:
    kind: SandboxWarmPool
    listKind: SandboxWarmPoolList
    plural: sandboxwarmpools
    singular: sandboxwarmpool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxWarmPool is the Schema for the sandboxwarmpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SandboxWarmPool
            properties:
              replicas:
                description: Replicas is the desired number of warm Sandboxes.
                format: int32
                minimum: 0
                type: integer
              templateRef:
                description: TemplateRef references the SandboxTemplate used to create
                  warm Sandboxes.
                properties:
                  name:
                    description: Name is the name of the SandboxTemplate.
                    type: string
                required:
                - name
                type: object
            required:
            - replicas
            - templateRef
            type: object
          status:
            description: status defines the observed state of SandboxWarmPool
            properties:
              readyReplicas:
                description: ReadyReplicas is the number of warm Sandboxes whose phase
                  is Running.
                format: int32
                type: integer
              replicas:
                description: Replicas is the number of warm Sandboxes currently in
                  the pool.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
